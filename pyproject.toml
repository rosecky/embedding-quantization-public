[project]
name = "eq"
version = "0.1.0"
description = "Data layer and metrics for the retrieval-quantization research project"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
